config:
  namespace: argocd

argo-cd:
  configs:
    params:
      server.insecure: true
    timeout:
      reconciliation: 10s
    credentialTemplates:
      github:
        url: "{{ .Values.argo-cd.configs.credentialTemplates.github.url }}"
        password: "{{ .Values.argo-cd.configs.credentialTemplates.github.password }}"
        username: "{{ .Values.argo-cd.configs.credentialTemplates.github.username }}"
    repositories:
      app-configs:
        url: https://github.com/shuffleSoftware/app-configs
      cluster-addons:
        url: https://github.com/shuffleSoftware/cluster-addons
  server:
    deploymentAnnotations: {}
    ingress:
      enabled: true
      hostname: "argocd-dev.homelab.com"
      ingressClassName: "nginx"
    config:
      admin.enabled: 'true'
      statusbadge.enabled: "true"
      users.anonymous.enabled: "true"
